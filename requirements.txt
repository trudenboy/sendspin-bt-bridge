sendspin==7.0.0
aiosendspin[server]==5.1.1
flask==3.1.3
waitress==3.0.2
dbus-python==1.4.0
dbus-fast==4.0.4
pulsectl-asyncio==1.2.2
music-assistant-client==1.3.5
websockets==16.0
zeroconf==0.148.0
PyJWT>=2.8.0
cryptography>=46.0.7
# Optional adapter recovery ladder (mgmt reset → rfkill → USB unbind/rebind).
# Consumed by services.adapter_recovery behind the
# EXPERIMENTAL_ADAPTER_AUTO_RECOVERY flag. Linux-only transitively via PyRIC;
# import failures are caught and the feature fails soft.
bluetooth-auto-recovery==1.5.3

# Live RSSI for connected BR/EDR peers via BlueZ kernel mgmt socket
# (MGMT_OP_GET_CONN_INFO opcode 0x0031). Pure-Python wrapper so we don't
# hand-roll struct.pack of the request/response packets. Linux-only;
# services.bt_rssi_mgmt catches ImportError and returns None on non-Linux.
btsocket==0.3.0
