sendspin==7.0.0
aiosendspin[server]==5.1.1
flask==3.1.3
waitress==3.0.2
dbus-python==1.4.0
dbus-fast==4.0.4
pulsectl-asyncio==1.2.2
music-assistant-client==1.3.5
websockets==16.0
zeroconf==0.148.0
PyJWT>=2.8.0
cryptography>=46.0.7
# Optional adapter recovery ladder (mgmt reset → rfkill → USB unbind/rebind).
# Consumed by services.adapter_recovery behind the
# EXPERIMENTAL_ADAPTER_AUTO_RECOVERY flag. Linux-only transitively via PyRIC;
# import failures are caught and the feature fails soft.
bluetooth-auto-recovery==1.5.3
